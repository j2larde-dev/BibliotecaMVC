@model BibliotecaMVC.Models.Libro

@{
    ViewData["Title"] = "Detalles del Libro";
}

<h1>@Model.Titulo</h1>

<img src="~/images/@Model.Imagen" class="img-libro mb-3" alt="@Model.Titulo" />

<dl class="row">
    <dt>Autor</dt>
    <dd>@Model.Autor</dd>

    <dt>Categoría</dt>
    <dd>@Model.Categoria</dd>

    <dt>Precio</dt>
    <dd>@Model.Precio</dd>

    <dt>Disponible</dt>
    <dd>@(Model.Disponible ? "Sí" : "No")</dd>
</dl>

<a asp-action="Index" class="btn btn-primary">Volver al listado</a>
